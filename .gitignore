# secrets - never commit your real config
config.json

# runtime data
chat_history.jsonl
discord.log
debug_last.wav
power_usage.json

# python
__pycache__/
*.pyc
venv/
.venv/

# large model download (fetched by setup.sh) - match any vosk model folder,
# not just today's default, so switching models never risks committing 1-2GB+
vosk-model-en-us-*/
