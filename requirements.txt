# AI Club Assistant (Steve)
discord.py>=2.4.0     # 2.4+ needed for voice channel status (VoiceChannel.edit(status=...))
websockets>=12.0
psutil>=5.9.0     # CPU/RAM stats for the admin UI (GPU/VRAM uses nvidia-smi if present)

# Voice (/join): capture, transcription, TTS
discord-ext-voice-recv>=0.0.1b0
PyNaCl>=1.5.0
vosk
numpy>=1.24.0
scipy>=1.11.0
pyttsx3>=2.90
# ffmpeg must also be on PATH for TTS playback
